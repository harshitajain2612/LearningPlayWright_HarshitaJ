let a = "Test";

if(true){
    //console.log(a);
    //let a = "temp";//once we re-assign the value of a, it will not be able to access the value of a which is outside the block. It will only be able to access the value of a which is inside the block.
    console.log(a);

    
}