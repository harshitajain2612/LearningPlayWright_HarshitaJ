let selectedItem = null; // This is a null literal representing the absence of a value
console.log(selectedItem); // Output: null


let declaredOnly; // This variable is declared but not initialized, so it has the value undefined
console.log(declaredOnly); // Output: undefined