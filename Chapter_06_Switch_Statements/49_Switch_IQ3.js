let x =10;

switch(x){
    case 10: let b1 = 1;
    console.log(b1);
    break ;
    case 10: let b2= 1;
        console.log(b1);
    break;
}

//JS allows to have a duplicate case
//Java doesn't allow to have the duplicate case.